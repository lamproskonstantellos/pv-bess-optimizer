Changelog
=========

The repository tracks only the current release.  The full per-task
diff for the most recent release lives in ``docs/CHANGELOG.md``.

Current release: see :file:`pvbess_opt/__init__.py` for the version
string and ``docs/CHANGELOG.md`` for the short release log.
