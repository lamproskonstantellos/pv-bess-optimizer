# Documentation toolchain.
# Install with:
#     pip install -r requirements/docs.txt
#
# Pulls in base + solvers so autodoc can import the package.

-r base.txt
-r solvers.txt

sphinx>=7,<10
sphinx-rtd-theme>=2.0
