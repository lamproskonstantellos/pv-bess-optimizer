# Developer / CI tooling.
# Install with:
#     pip install -r requirements/dev.txt
#
# Includes everything from base + solvers so a fresh checkout can be
# linted, smoke-tested and run end-to-end without extra commands.

-r base.txt
-r solvers.txt

# Linters / formatters
# ruff is the CI lint gate; pyflakes / pycodestyle are kept for legacy
# editor configurations that invoke them directly.
ruff>=0.15,<1
pyflakes>=3.1
pycodestyle>=2.11

# Tests
# Cap below the next major above the currently resolved version (9.x) so
# a future pytest 10 with breaking changes is not pulled in untested.
pytest>=7.4,<10

# Type checking (CI gate).  Pinned so the gate is reproducible: an
# unpinned upgrade can flag previously-valid `# type: ignore` comments and
# break the gate unpredictably.  The stub packages let mypy resolve
# pandas / openpyxl / dateutil under the strict config in pyproject.toml.
mypy==1.19.1
pandas-stubs==3.0.3.260530
types-openpyxl==3.1.5.20260518
types-python-dateutil==2.9.0.20260518

# Dead-code gate (configured under [tool.vulture] in pyproject.toml).
vulture==2.16
