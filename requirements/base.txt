# Core runtime dependencies for the bess package.
# Install with:
#     pip install -r requirements/base.txt
#
# Floors are the majors the suite is tested against; upper bounds guard
# against untested future majors (pandas 4 / numpy 3 / etc.).

pandas>=2.2,<4
numpy>=1.26,<3
matplotlib>=3.10,<4
openpyxl>=3.1,<4
pyomo>=6.7,<7
python-dateutil>=2.8,<3
