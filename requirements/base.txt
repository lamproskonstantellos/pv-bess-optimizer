# Core runtime dependencies for the bess package.
# Install with:
#     pip install -r requirements/base.txt
#
# Pin minimum versions; upper bounds intentionally left open so the package
# stays compatible with current pandas/numpy/matplotlib releases.

pandas>=2.0
numpy>=1.24
matplotlib>=3.10
openpyxl>=3.1
pyomo>=6.6
